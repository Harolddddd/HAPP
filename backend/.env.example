DATABASE_URL="postgresql://postgres:postgres@localhost:5432/happ?schema=public"
JWT_SECRET="change-me-in-production"
PORT=3000

# Comma-separated list of allowed origins for CORS. Leave unset for local
# dev (reflects any origin). Set to the deployed web origin in production,
# e.g. CORS_ORIGIN="https://app.example.com"
# CORS_ORIGIN=
